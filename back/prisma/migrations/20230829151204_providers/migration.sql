-- CreateTable
CREATE TABLE "Provides" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "phone" TEXT NOT NULL,

    CONSTRAINT "Provides_pkey" PRIMARY KEY ("id")
);
